<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Romjan HD Media</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div class="admin-login" id="adminLogin">
    <h2>Admin Login</h2>
    <input type="password" id="adminPassword" placeholder="Enter Admin Password">
    <button onclick="login()">Login</button>
  </div>

  <div class="main-app" id="mainApp" style="display:none;">
    <h1>📺 Romjan HD Media TV</h1>

    <!-- Video Upload Section -->
    <div class="upload-section">
      <h2>📤 Upload Video</h2>
      <input type="file" id="videoFile" accept="video/*">
      <button onclick="uploadVideo()">Upload</button>
    </div>

    <!-- Video List -->
    <div class="video-list" id="videoList">
      <h2>🎬 Video Library</h2>
    </div>

    <!-- Ad Section (Google AdSense Placeholder) -->
    <div class="ads">
      <h3>🔸 Ads</h3>
      <!-- Replace with real AdSense code when live -->
      <iframe src="https://www.google.com" style="width:100%; height:100px;"></iframe>
    </div>
  </div>

  <script src="script.js"></script>
</body>
</html>
body {
  font-family: Arial, sans-serif;
  background: #fff8e1;
  color: #704200;
  text-align: center;
  padding: 20px;
}

h1, h2 {
  color: #b8860b; /* sunali color */
}

.upload-section, .video-list, .ads {
  margin: 20px auto;
  padding: 20px;
  border: 2px solid #daa520;
  border-radius: 12px;
  background: #fffaf0;
  width: 90%;
  max-width: 600px;
}

button {
  padding: 10px 20px;
  background-color: #ffd700;
  border: none;
  border-radius: 8px;
  cursor: pointer;
  color: #000;
}

button:hover {
  background-color: #ffc107;
}

video {
  width: 100%;
  max-height: 300px;
  margin-top: 10px;
}
const correctPassword = "01849360175";

function login() {
  const enteredPassword = document.getElementById("adminPassword").value;
  if (enteredPassword === correctPassword) {
    document.getElementById("adminLogin").style.display = "none";
    document.getElementById("mainApp").style.display = "block";
    loadVideos();
  } else {
    alert("Wrong password!");
  }
}

function uploadVideo() {
  const fileInput = document.getElementById("videoFile");
  const file = fileInput.files[0];
  if (!file) return alert("Select a video file first.");

  const videoURL = URL.createObjectURL(file);
  const videoList = document.getElementById("videoList");

  const videoContainer = document.createElement("div");
  videoContainer.innerHTML = `
    <video controls src="${videoURL}"></video>
    <br>
    <button onclick="this.parentElement.remove()">Delete</button>
  `;
  videoList.appendChild(videoContainer);

  fileInput.value = "";
}

function loadVideos() {
  // Future use: load from server or Firebase
}
